export default function Thread(props) {
    const shortName = props.shortName;
    const threadId = props.threadId;

    return ( // todo
        <p>shortName: {shortName}, threadId: {threadId}</p>
    );
}